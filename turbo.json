{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "proto:generate": {
      "outputs": ["service/gen/**", "ui/mobile/gen/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "outputs": ["dist/**", "build/**"]
    }
  }
}